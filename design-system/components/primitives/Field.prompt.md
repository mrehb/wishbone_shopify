# Field

Text input as a single underline. The label sits above in 12px caps grey; the line is the hairline, becomes ink on focus, and becomes a 2px lime line on error, with the instruction written plainly beneath (*Add the part after the @.*). There is no red in the system and no boxed input; a multiline field is the same underline with a textarea.
