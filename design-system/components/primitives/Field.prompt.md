# Field

A pill-shaped input with a mono label above it. Placeholders are examples, never labels.

There is no red in this system. An error is the field's border going lime and a `▸` line beneath
that says what to do — *"▸ Add the part after the @ — for example you@example.com."* Lime means
"attention here", and the instruction is the fix.
