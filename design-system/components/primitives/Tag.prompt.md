# Tag

The compatibility chip. A hairline rectangle with a 12px caps model name; the selected or matching model is filled lime. Used on parts (which model a part fits) and in the compare table header. Never rounded, never coloured any other way.
