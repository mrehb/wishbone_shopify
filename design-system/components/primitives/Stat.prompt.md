# Stat and StatRow

A measured fact: the number in light Manrope at 44px, the unit at half size in grey, the label beneath in 12px caps. Unknown values print an em dash with *not yet measured* underneath — never a guess, never a blank. StatRow sets up to five stats side by side, centred, divided by hairlines, and is the band that follows every hero.
