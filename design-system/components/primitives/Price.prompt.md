# Price

The one thing set in Manrope 600. Formatted as the store formats it — `799,00 €` — with an optional grey `FROM` label before a range's lowest price and a struck-through compare-at price after. Unknown prints an em dash.
