# Readout

The number. 48px dot-matrix by default, unit in small caps beside it, one line of context below.

Pass an unknown value and it prints **—** in grey with *not yet measured* beneath — never a guess,
never a hidden row. Fold size and weight are still unknown for every model; a page of readouts
showing `—` is the honest state and the reminder to fix it.
