# Specs

The specification as a hairline table: label in grey caps on the left, value on the right, one row per fact. Rows whose value is unknown are still listed and print an em dash with *not yet measured* — fold size and weight for every model today. The left column carries the headline and, on a product page, the in-the-box list with quantities.
