# Lineup

The reference's "built to perform" band. Four columns of copy — eyebrow, headline, one paragraph, an outline button — then up to three products on mist tiles with `WISHBONE ONE`, the type in grey, and `FROM 229,00 €`. Products without a photograph draw their silhouette. Never more than three across; a fourth model becomes a second Lineup or a Compare.
