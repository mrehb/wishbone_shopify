# PartTile

Fifteen of the twenty-two parts have no photograph. Rather than a grey box, every part tile carries
a dot ring — a part is a component of a whole — and the **compatibility chips** below the title do
the work an image would: a customer scanning for a NEO wheel reads the chip first.

Titles keep their `(MODEL)` suffix in full. Shorten the tile, never the name.
