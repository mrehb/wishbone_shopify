# SpecTile

One value per tile — range, weight, fold size, charge time, part count — as a readout with the dot
graphic that suits it: bars for a quantity, a ring for a percentage. Product pages are a board of
these. An unknown value prints `—` with *not yet measured*; that is the honest state for fold size
and weight on every model today, and the reason to go and measure them.
