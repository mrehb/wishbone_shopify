# CompareTile

Five models is few enough to compare in one tile — so the site should, instead of making a customer
open five tabs. Missing values render as grey `—`, never blank and never guessed; the model being
viewed is the only lime on the table. Carry a *parts listed* row — nobody else publishes it.
