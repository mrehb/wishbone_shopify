# SiteFooter

Four columns on the page ground — no separate footer surface, since the ground already is the
brand. Column headings in mono caps, links in body type at 80 % opacity so the footer reads as
reference rather than a second navigation.

The wordmark repeats at 150px with *Birthed in Britain* — the only place the brand statement
appears outside the homepage.
