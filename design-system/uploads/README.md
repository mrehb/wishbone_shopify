# Raw sources

Extracted from the live store on 2026-09-19 — the evidence behind every value in this system.

- `BRAND_KIT.md` — the full derivation, with each rule labelled **Observed** (read from the store)
  or **Proposed** (written to make the system consistent).
- `tokens.css` / `tokens.json` — the same values as CSS custom properties and W3C design tokens.
- `wishbone-wordmark.png`, `wishbone-monogram.png` — the two marks as they exist in Shopify Files.

Canonical copies live in the theme repo: `github.com/mrehb/wishbone_shopify` under `docs/brand/`.
